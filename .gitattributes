# Add the file to git
git add .gitattributes

# Remove and re-add all files to apply line ending rules
git rm --cached -r .
git add .

# Commit the changes
git commit -m "chore: add .gitattributes for consistent line endings"

# Push to GitHub
git push origin main